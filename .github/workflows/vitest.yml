name: Vitest

on: [push, pull_request]

jobs:
    unit-tests:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4

            - uses: actions/setup-node@v4
              with:
                  node-version: 20
                  cache: npm

            # Install dependencies, skipping optional deps like fsevents on Linux
            - run: npm ci --omit=optional

            # Run unit tests
            - run: npm run test -- --run
